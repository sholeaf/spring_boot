<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.PBoardMapper">
	<select id="getList">
		select * from p_board
		where boardnum > #{lastBoardnum}
		order by boardnum desc
		limit #{limit}
	</select>
</mapper>


